# Administration workflow overhaul

This continues beyond the shared shell refresh. Each destination needs a review of its real workflows, a tailored implementation, an assessment of useful missing capabilities, and verification of the result. A header change does not complete an area.

The visual direction is an editorial workspace: quiet surfaces, concise context, prominent task controls, readable records and progressive disclosure of advanced settings. Configuration state must be distinguishable from observed operational health.

| Area | Workflow and capability scope | State |
| --- | --- | --- |
| Pages | Inventory, filtering, ownership, publication, detail and bulk actions | Implemented; first milestone verified |
| Tags | Taxonomy, usage, editing and consolidation | Implemented; first milestone verified |
| Editors | Availability, recommendations, format usage and authoring entry | Implemented; first milestone verified |
| Rendering | Module configuration, pipeline inspection and stored output | Implemented; first milestone verified |
| Comments | Providers, discussion and moderation | Implemented; first milestone verified |
| Users | Discovery, details, creation and access lifecycle | Implemented; first milestone verified |
| Groups | Membership, permissions, page rules and effective access | Implemented; first milestone verified |
| Authentication | Providers, sign-in, provisioning and diagnostics | Verified and deployed (provider workspace, provisioning, initialization, reviewed saves) |
| Security | Sessions, protections and policies | Deployed and verified; five policy sections, reviewed persistence, session effects and shared password enforcement |
| Wiki Agent | Providers/models, skills, browser, tools, memory, MCP and runtime | Implemented; first milestone verified |
| Search | Providers, retrieval, index lifecycle and evaluation | Implemented; first milestone verified |
| API | Credentials/scopes, integration setup and reference | Implemented; first milestone verified |
| Webhooks | Events, endpoints, deliveries, tests and recovery | Implemented; first milestone verified |
| General | Identity, announcements, features and defaults | In progress; transactional foundation and publication-window behavior verified; interface/legacy integration pending |
| Theme | Appearance, previews and reversible editing | Pending |
| Navigation | Structure, ordering, audiences and preview | Pending |
| Locale | Languages, defaults and multilingual operation | Pending |
| Analytics | Providers, insight and collection controls | Pending |
| System | Environment, health and diagnostics | Pending |
| Storage | Targets, synchronization, recovery and job state | Pending |
| Mail | Sender, transport and delivery diagnostics | Pending |
| SSL | Certificates, renewal and ingress responsibilities | Pending |
| Logging | Destinations and troubleshooting | Pending |
| Extensions | Availability, configuration and dependencies | Pending |
| Utilities | Import, export and maintenance workflows | Pending |
| Developer flags | Constraints, dependencies and activation | Pending |
| GraphQL explorer | Integration, authentication and exploration | Implemented; first milestone verified |

## Wiki Agent findings

The existing console has working provider creation, connection checks, access grants, default selection, skill approval and browser target management. Its landing view prioritizes deployment limits, repeats status in several places and leaves no clear setup path. Providers have no inventory filtering. Actual tool exposure and memory behavior are absent from administration, and MCP setup is disconnected from the capability policy that governs it.

The redesign starts with actionable setup status, brings providers forward, makes sections directly addressable, and introduces an authoritative tool inventory generated from the server action catalog and deployment flags. This inventory describes deployment eligibility; individual permissions, selected skills, provider support and execution-time approvals still apply. Personal memory remains private to its owner. Deployment kill switches remain deployment configuration.

Verification and implementation evidence will be recorded as each area is completed. All areas require final browser review, appropriate checks and deployment verification before this goal is complete.

### Agent implementation and verification

- Seven directly addressable sections: overview, providers, skills, browser access, tools/MCP, knowledge/memory and runtime. Full-width working area replaces the nested sidebar and repeated status cards.
- Setup checks distinguish runtime configuration, enabled/verified provider credentials and an eligible shared default. This is configuration evidence, not a live inference-health claim.
- Provider filtering covers names, models, endpoints, protocols and audiences; state filters expose connections needing attention. Existing-profile editing supports direct section access and saving from any section. Connection history uses the existing bounded verification endpoint, including individual probe results and retry.
- Browser destinations have search and state filters. Existing exact-URL approval and pause workflows remain enforced.
- A new administrator-only skill source endpoint finds unmapped Markdown roots within the configured namespace. Search treats wildcard characters literally, caps results at 20 and excludes nested/unrelated pages. The picker fills source references and the matching skill name; existing server validation and exact-revision approval remain authoritative.
- Runtime now projects tool descriptors and transport-specific deployment blockers directly from the action catalog. The new tool directory filters by capability, interface and eligibility and explains permissions/effects. Personal memory is explicitly unavailable over MCP. MCP currently exposes the wiki to clients; arbitrary outbound MCP server execution is not part of the runtime architecture and no nonfunctional connection editor is offered.
- Knowledge/memory explains authoritative pages, approved skills, owner-private memory snapshots and the actual retention configuration. Deployment gates and personal-memory ownership remain intact.

Validation: client/shared/server type checks, repository lint, Agent component tests, administration integration tests and server controller/catalog tests. Browser preview reviewed all seven sections at 1440px and 390px in dark and light themes: no page overflow, JavaScript errors or WCAG A/AA violations after corrections. Interactive checks covered provider and destination filters, history, direct provider editing/discard, source selection, tool disclosure and section persistence after reload. Source selection used controlled metadata without creating a production skill. Deployed milestone `d49ac379` passed a second browser review without asset or API interception: all seven sections at desktop/phone in both themes, real tool policy and source-search responses, provider filtering/history, source-search empty state, section persistence and keyboard navigation. Container health is healthy. Final cross-administration review remains pending with the other areas.

## Search findings

Initial code review confirms that engine selection/configuration and rebuilding work, but Apply is enabled even without changes; no draft reset or query evaluation is available. The next implementation should separate saved engine state from configuration drafts, make maintenance understandable, and add a real query-evaluation workflow showing results and match evidence from the actual search service. Index health must be measured from supported server state, never inferred from a selected engine alone.

### Search implementation and verification

- Three directly addressable workspaces: Configuration, Evaluate queries and Index maintenance. Engine configuration has a saved baseline, dirty gating, reset, an accessible leave confirmation and a save bar outside the panel transition container.
- Query evaluation uses the same permission-filtered page-search API as readers. It displays the submitted query/scope, actual matching fields, relative scores, spelling suggestions, round-trip duration, bounded-window guidance and further result pages. Draft configuration never silently changes an evaluation.
- A new system-administrator endpoint projects PostgreSQL index coverage: published public pages, indexed entries, missing pages, stale source revisions and entries that no longer belong in the public index. Dictionary and schema metadata are compared with the running engine. Inspection uses one query snapshot with a five-second statement timeout and makes no index changes. Unsupported inspection is explicit, not an empty healthy result.
- Rebuilding has a review step, server-confirmed completion, an explicit uncertain-outcome state after a failed request and guidance that leaving the page does not cancel the server operation. Inspection is on demand; this is not a persistent rebuild-job history or a latency/service-health monitor.
- Enumerated configuration settings are validated before any engine rows are changed, preventing an invalid dictionary selection from being persisted through the API.

Verification: draft/reset/save-failure/navigation tests; REST transport and schema tests; administrator access and error-redaction tests; engine inspection tests; shared/client/server type checks, lint, build and bundle budgets. The exact inspection SQL was also executed against isolated PostgreSQL temporary tables covering current, missing, stale, private, unpublished and orphaned entries. All three sections passed browser preview at 1440px, 900px and 390px in light/dark themes with no page overflow, JavaScript errors or WCAG A/AA violations. Interactive checks covered a real query, configuration reset, navigation cancellation, inspection failure/retry, rebuild review cancellation and section persistence. Production verification is the next gate.

Search deployment `dd87cd09` is healthy. Unintercepted browser verification covered all three sections at 1440px, 900px and 390px in light and dark themes with no overflow, JavaScript errors or WCAG A/AA violations. Real queries, inspection and a server-confirmed index rebuild passed; the post-rebuild snapshot reported zero missing, stale or excluded entries with matching dictionary metadata.

## Webhooks findings

The old endpoint editor required manually typed event subscriptions, allowed silent draft loss when changing selection, and displayed delivery state without the stored error, response or retry schedule. Actual page producers emit hyphenated names that subscription validation rejected. There was no targeted receiver test or embedded integration contract.

### Webhooks implementation and verification

- Endpoint directory with name/URL search and status filtering; a compact mobile chooser. Each selected endpoint has directly linked Setup, Deliveries and Receiver guide sections, with endpoint identity preserved in the URL.
- Destination and subscription steps, actual page/review event catalog, custom subscription support, and matching server/client validation for hyphenated event names. New endpoints start disabled while their receiver signing secret is configured. Existing API callers retain the prior default behavior.
- Saved/draft separation, change-gated save/reset, navigation and endpoint-switch protection, recoverable failed saves, and protected one-time signing secret display. Rotation and deletion clearly explain consequences for queued and in-flight deliveries.
- Delivery search/state filters and expandable response, error, identifiers, timestamps and next-attempt details. Retry and cancellation use the existing durable state machine. Disabled deliveries are identified as skipped instead of implying a receiver acknowledged them.
- New administrator-only POST `/_api/webhooks/:id/test` queues a targeted `webhook.test` event containing only a synthetic marker/message. It uses the same signed worker delivery path, attempts once, rejects disabled/missing endpoints and duplicate active tests, and cannot fan out to other subscribers. The PostgreSQL endpoint lock serializes concurrent test creation; all inserts are transactional.
- Receiver guide documents the actual envelope, signature inputs/headers, timeout, acknowledgement, idempotency, metadata access and queue-history retention. Responses are private/no-store; unexpected server errors are logged and redacted from the client.
- Capability limits: history retains only the latest response per delivery and is bounded to the latest 100 records; terminal queue cleanup can remove history after 30 days. There is no durable per-attempt timeline, automatic UI polling, per-page webhook ACL or permanent webhook audit archive. These limits are explicit in the interface.

Validation: targeted queue, operation, authorization, subscription, worker and client workflow tests; shared/client/server type checks; lint; Vite build. Preview browser review covered three sections at 1440px, 900px and 390px in light and dark themes with no overflow, JavaScript errors or WCAG A/AA violations. Controlled fixtures exercised create/save/failure/reset, event selection, endpoint and route draft guards, secret acknowledgement/rotation, test queueing, delivery retry/cancel, refresh recovery, deletion and bookmark restoration. No test messages were sent to external receivers during verification.

The targeted test transaction also passed against PostgreSQL temporary copies of the four relevant tables: one queued delivery, synthetic event marked published, active-test conflict, and disabled-endpoint rejection. No worker was invoked and no live rows were modified.

Webhooks deployment `f247c675` is healthy. Unintercepted browser verification covered all three sections at desktop, tablet and phone widths in both themes with no overflow, JavaScript errors or WCAG A/AA violations. A temporary disabled endpoint verified real creation, persisted hyphenated subscriptions, rotation, deletion, draft protection and server-side rejection of a test while disabled. The fixture was removed. No outbound delivery was sent. Detailed delivery recovery and synthetic sends used controlled browser fixtures plus queue/worker tests, not third-party receivers.

## API access / GraphQL explorer initial findings

The API inventory counts expired keys as active, hides issued group permissions and MCP resource bindings, and mixes key administration with a long integration reference. Key generation has permission selection but no preview of the selected group's effective grant. When MCP is enabled, newly issued keys are automatically bound to its configured resource; the existing reference does not clearly explain this lifecycle or how older keys differ. GraphQL exploration is provided by the server's GraphiQL endpoint, permission-gated separately from API-key enablement. The implementation separates credential administration from connection setup and preserves the standalone schema-aware editor.


### API access implementation and verification

- Three directly linked sections: Credentials, Connect a client and GraphQL explorer. Searchable credential register distinguishes active, expired, revoked and unknown expiry; expandable records show issued group identity, current group grants, dates and MCP resource binding. Global API state is separate from credential validity.
- Guided Identity / Access / Review issuance with a 90-day default, non-system group selection, current permission/page-rule preview and explicit MCP opt-in. Replacement carries forward scope and MCP intent, and leaves the old credential valid until separately revoked. One-time credentials remain visible even if inventory refresh fails. Draft/error recovery and navigation protection cover issuance and secret acknowledgement.
- Administrator-only connection metadata exposes configured MCP resource and group grants without secrets. Existing callers that omit MCP intent retain automatic binding behavior; new UI callers can explicitly exclude MCP. Creation validates lifetime, group existence and explicit MCP configuration. API responses are private/no-store. Failed global-state persistence restores runtime state; concurrent state writes serialize.
- Protocol-specific REST, GraphQL and MCP connection guidance uses real endpoint/configuration data, copyable examples with environment-variable placeholders, grant requirements and authentication diagnostics. Configuration evidence is explicitly distinguished from a successful connection test.
- Limits: there is no fabricated last-use telemetry, per-credential request history or immutable permission snapshot. Scoped credentials continue to inherit their group's current access. Protocol entitlement is bounded by actual server authentication rules; MCP resource binding is separate from the group's use:mcp permission.

Validation: 48 controlled browser cases across three widths and both themes, including all issuance stages and protocol guides; no overflow, JavaScript errors or WCAG A/AA violations. Lifecycle checks exercised creation, replacement, issuance failure, refresh failure without losing the secret, revocation and global state changes. Eleven targeted test files passed, along with shared/client/server type checks and repository lint. Production lifecycle verification is pending deployment.

### GraphQL explorer implementation and verification

- Integrated workspace navigation and a collapsible guide with three read-only starter queries validated against the actual server schema. Signed-in session authentication, optional bearer headers, real mutation effects and local query-history behavior are explained. Header persistence is disabled.
- Retains the full Yoga GraphiQL editor, query execution, variables, headers, schema documentation and subscriptions. Supplies an immediately executable page inventory instead of a welcome comment. Document selection/closing retains native React handlers and exposes independent accessible controls with keyboard navigation.
- Ships the pinned Yoga IDE 4.4.4 script, styles and Monaco workers as same-origin container assets. Build-time version checks prevent mismatched bundles. The build copies the MIT notice and the license inventory accounts for the prebuilt renderer's dependencies. Exact reviewed peer attribution exceptions record upstream package-range mismatches without changing application dependency resolution.
- Responsive workspace chrome, readable dark editor annotations, visible documentation links and theme-aware guide controls. No external script/style dependency is needed to open the editor.

Validation: six responsive/theme browser cases with no overflow, JavaScript errors or WCAG A/AA violations; actual permission-filtered page query, schema documentation, multiple documents and keyboard selection. Asset lifecycle/version mismatch tests, schema validation, subscription configuration regression tests, dependency and license checks passed. Production verification is pending deployment.


API/GraphQL deployment `09d11068` is healthy and its container revision matches the committed source. The production Docker build, bundle gates, dependency policy and 809-record license inventory passed. Unintercepted API browser verification covered Credentials, Connect and Explorer at desktop, tablet and phone widths in both themes with no overflow, JavaScript errors or WCAG A/AA violations. A temporary scoped credential was issued through the UI, authenticated a real REST page-list request (HTTP 200), and was revoked through the UI; a subsequent request was rejected. A second temporary scoped credential verified explicit MCP resource binding and successful MCP initialization. Both credentials were revoked, and the original global API state was preserved. Revoked verification rows remain as lifecycle records.

The live GraphQL workspace passed the six width/theme cases, a real page inventory query, schema documentation and keyboard selection across multiple documents. It loaded its scripts/styles from the same container with no external UI dependency. Verification browser authentication state was removed after testing. Rollback configuration is retained locally as `compose.before-09d11068.yml`.

## Pages initial findings

The inventory supports basic local text, locale and publication filtering, but omits available visibility, private ownership, content type and taxonomy context. It has no selection or bulk workflow. The detail page mixes operational metadata and destructive actions with authorship, privacy and knowledge metadata, making routine review difficult. The list API already applies requester access and returns visibility, owner ID, content type and tags; these must remain permission-filtered as the administration view becomes richer. Next work should build an information-rich inventory, contextual detail sections and justified bulk/review capabilities with explicit scope and recovery. Pages remains pending substantive implementation and live verification.


### Pages implementation and verification

- Rebuilt the inventory as a readable knowledge register with quick views, text/language/visibility/publication/tag filters, explicit sorting, bounded pagination and bookmarkable filter URLs. It exposes actual publication windows, private owner IDs, content type and taxonomy alongside each page. Counts describe accessible loaded pages; published/private states are kept distinct. Filtered inventory exports to JSON.
- Selection spans filtered views with an explicit hidden-selection count and a 25-page review limit. Bulk publication fetches fresh page snapshots before confirmation, preserves visibility/content/ownership/schedules, runs sequentially and reports each outcome. Stop waits for the current request, then leaves remaining pages available to resume. Failed pages must be reviewed again; successful pages are not automatically repeated. Browser navigation is guarded while work is active.
- New PATCH `/_api/pages/:id/publication` validates booleans, canonical revision input and ISO schedule boundaries, including invalid calendar dates and end-before-start. It permits enabling/disabling without changing a schedule, or explicitly setting/clearing both boundaries. Writes delegate to the established editor operation/model for requester access, password protection, revision conflict checks, history, indexing, rendering and durable events. Schedule fields in inventory responses remain restricted to callers entitled to publication metadata.
- Three detail sections: Overview (identity, authorship, ownership and revision links), Publication (saved/draft separation, local-time schedule editor, state preview, reset and recoverable save errors), and Knowledge & provenance (authoritative metadata, trust/review state, source records, derived summary, completeness, open questions and inspectable projection data).
- Reviewed visibility changes and system-administrator ownership transfer now use the existing protected endpoints. Account search, explicit access acknowledgement, collision/conflict errors and draft/navigation protection support the workflow. These controls do not conflate publication with workspace visibility. Deletion remains an explicit, revision-checked action in the technical section.
- Reimagined the visualization as a page atlas with locale scope, structural context, relation counts, reset, three diagram modes and a searchable keyboard-accessible connection directory. Diagram links resolve directly to page administration. No existence claim is made for paths outside the accessible dataset.
- Capability limits: inventory filtering/sorting is client-side over the accessible list; there is no server-paginated large-estate inventory yet. Bulk processing belongs to the active browser, not a durable background job; closing it can lose the displayed outcomes. Bulk editing currently changes publication enablement, not ownership, visibility, tags or page deletion. Account search returns up to ten matches and does not imply an account is active. Review/approval and content authoring remain in the existing page/editor workflows.

Validation so far: 54 browser cases across desktop/tablet/phone widths and both themes passed without overflow, JavaScript errors or WCAG A/AA violations. Focused interactions passed bookmark restoration, filtered export, bulk stop/resume, partial failure review/retry, single-page conflict recovery and navigation protection. Ten targeted test files passed, covering the page transport, protection delegation, schedule validation, revision/conflict behavior, private-page error policy, existing routed load/delete races and graph loading. An additional 24 access-dialog and populated knowledge/provenance cases passed across the same widths and themes, including ownership transfer and a visibility conflict. Production deployment remains pending.


Pages deployment `e3cef27f` is healthy and its image revision matches the committed source. Production Docker build and bundle gates passed. Unintercepted browser verification covered the register, three detail sections, atlas and connection directory in 36 desktop/tablet/phone and theme cases with no overflow, JavaScript errors or WCAG A/AA violations. Two temporary private pages verified real UI bulk publication, persisted schedule boundaries, a stale-revision HTTP 409, invalid-calendar-date HTTP 400, and return to draft. Both remained private with unchanged owners and were deleted after verification. No enabled webhook receivers were present. Visibility changes and ownership transfer were exercised through controlled browser fixtures and existing protected operation tests; no real page was reassigned to another person. Browser authentication state was removed after testing. Rollback configuration is retained locally as `compose.before-e3cef27f.yml`.

## Tags initial findings

The editor assigns the selected list object directly to its form, so unsaved edits alter the directory and a failed save can look persisted. It has no saved/draft separation, navigation protection, usage overview or consolidation workflow. Its list derives tags from accessible pages and hides orphaned tags. Server rename/delete operations change tag rows/associations directly without an explicit page-projection/cache refresh or an impact review. Group rules can match a tag's name with `TAG` and give those rules high specificity; renaming or consolidating a tag can therefore change effective access. The next implementation needs a taxonomy inventory, draft-safe editing, associated-page and rule impact review, and transactional consolidation/cleanup that preserves page and authorization invariants. Tags remains pending substantive implementation and verification.


### Tags implementation and verification

- Replaced the shared-object form with a vocabulary directory and contextual Definition, Usage & access, and Lifecycle sections. Search, active/unused/alias/archived views, pagination, selected-tag URLs, usage links, alias navigation, history-reference counts and language-aware group-rule references make the scope visible. Definitions have independent drafts, validation, reset, navigation protection and recoverable errors. Creation reserves an unused name without requiring a placeholder page.
- Added a system-administrator taxonomy API with inventory, inspection, create, impact preview and reviewed apply. A deterministic review fingerprint covers the taxonomy, group rules and relevant source/destination page populations and revisions. It rejects stale reviews, duplicate/reserved names and invalid lifecycle transitions. A separate acknowledgement is required when public-page matches change for any affected tag rule; counts are not an effective-access simulation.
- Renames create a canonical destination and preserve the old row/name as an alias; merges deduplicate page assignments and flatten aliases. Archiving retires a name without deleting historical references; canonical retirement removes active assignments and archives inbound aliases. Restoration makes a name available again without reconstructing old assignments. Archived aliases require an active destination before restoration.
- Tag-based authorization resolves aliases without rewriting group-rule strings or changing their original specificity. Archived names fail closed even when older page labels remain in cache. Authoring resolves historical names under shared tag locks and rejects archived labels. Tag-filtered REST/GraphQL page lists resolve historical names, and the reader retains a visible, removable selection chip for names absent from its current tag directory.
- Assignment changes commit page history, revisions, knowledge metadata, durable render/link/search/knowledge projections and the existing page.updated outbox event in the same transaction. Private visibility, ownership, content and publication are preserved. Page → tag lock ordering and a second fingerprint check reject assignments committed while the change waits for authoring locks. Existing system-administrator protection policy is retained. Label-only edits refresh page caches without inventing content history. Post-commit refresh failures return a saved result with warnings.
- Capability limits: inventory/inspection currently read the workspace taxonomy and tagged-page metadata into memory, and directory filtering is client-side. A lifecycle write is one database transaction, not a resumable background taxonomy job. Public-page rule counts include locale filtering but do not simulate combined group permissions or rule precedence. Alias resolution covers authoring, tag-rule authorization and explicit tag filters; it does not expand arbitrary full-text search queries. Historical names remain reserved; there is no permanent-purge control. Cross-process cache invalidation uses the existing event bus.
- Migration adds alias targets and archive state. Roll-down is refused while lifecycle data exists. A pre-migration PostgreSQL custom-format backup was captured and its archive listing verified locally; changing to an older image alone is insufficient after taxonomy lifecycle writes.

Validation: 36 responsive/theme browser cases passed without overflow, JavaScript errors or WCAG A/AA violations. Interactions passed create/duplicate validation, saved-versus-draft isolation, navigation protection, rename → merge → archive → restore, explicit access acknowledgement, stale-review refresh and list/detail retries. PostgreSQL integration tests passed concurrent normalized authoring, alias resolution, archived-name rejection, transactional history/projections/events, private ownership preservation, deduplication, stale page/rule/assignment reviews, a real shared-lock race, protection/history rollback, saved cache-failure reporting and rollback guarding. Focused authorization, legacy transport, history, page-model and migration regressions passed. Repository lint and shared/client/server type checks passed. Production deployment and unintercepted verification remain pending.


Tags deployment `2cb5fc62` is healthy and its image revision matches the committed source. The initial image was rejected before migration because the filename used the frozen historical namespace; the previous healthy container was restored, the migration was registered as `tsepistle-000015-taxonomy-lifecycle`, and the repository migration-inventory/namespace contract passed before redeployment. Production Docker builds and bundle gates passed.

Unintercepted verification passed 30 directory/detail/review width/theme cases without overflow, JavaScript errors or WCAG A/AA violations. Real workflows passed anonymous API denial, tag creation, private-page rename/history/revision preservation, old-name page filtering, merge, stale-review HTTP 409, archive and restoration. An empty temporary group verified real tag-rule references while private pages correctly contributed zero public-rule matches. No existing group memberships or permissions changed. Both temporary private pages and the group were removed; their three verification names remain archived to retain historical references. All 32 projection effects for the verification pages succeeded. An archived reader bookmark retained a visible selection chip and returned no page assignments. Access-broadening acknowledgements were exercised in PostgreSQL and browser fixtures; the live test did not broaden a real person’s access. The local pre-migration backup and `compose.before-2cb5fc62.yml` are retained.

## Editors initial findings

The existing subsection changes new-page editor availability through site configuration and preserves existing-page editing. It has five format/editor choices, last-editor protection, select-all and restore-saved controls. Its large colored toolbars and explanatory cards dominate a small configuration job; it does not show actual content usage, a creation-flow preview or a workspace recommendation/default. Failed saves only raise a global notification, and unsaved edits have no route/unload guard. Cards and switches remain interactive during a save, which can leave the visible selection different from the snapshot just persisted. A legacy per-user defaultEditor field exists in the data model, so any new workspace default must account for that distinction and the actual new-page chooser rather than adding an inert setting. Next work should review the authoring entry points, compare editor capabilities and content types, show real usage, and build a cohesive configuration/review experience with robust persistence and recovery. Editors remains pending implementation.


Final Tags image `0926086b` is healthy. Its production build/bundle gates and client type check passed. A final live test held a detail response, cleared the selection, and verified that the welcome state returned immediately and the late response did not overwrite it. The temporary browser authentication-state file was removed after verification. `compose.before-0926086b.yml` retains the preceding migrated image; the earlier pre-migration backup remains the recovery point for reverting taxonomy semantics.


### Editors implementation and verification

- Three directly linked sections: Creation policy, Formats & usage and Author preview. Five editor choices show actual page/private-page usage and registry availability. The format comparison explains stored source, editing surface, preview and actual collaboration support; internal editor types are counted separately.
- Added a persisted workspace recommendation that places an enabled editor first in the real new-page chooser. It is optional and does not override authors’ choices. One available editor retains the existing direct-entry behavior. Hiding the recommended editor clears the recommendation explicitly; existing pages keep their editor and source.
- Independent drafts, reset, navigation protection, saved-versus-draft previews and a reviewed change summary replace the old immediate configuration form. Controls lock during review/save, failures retain the draft, and stale settings offer a deliberate reload path.
- New administrator-only editor API persists only the editor settings row. PostgreSQL transaction/advisory locking and revision fingerprints reject concurrent and stale writes; unknown configuration fields survive. Runtime activation occurs after commit and reports refresh warnings honestly. Legacy availability writes use the same persistence path; unrelated site saves cannot overwrite editor settings. Failed generic configuration persistence now reports failure.
- Rebuilt the author-facing chooser with responsive, keyboard-operable cards, readable format labels and the actual saved recommendation. Template entry checks the source editor before navigation and preserves the destination’s private namespace, fixing the previous private-to-public URL reconstruction. Existing server source-access and new-page availability enforcement remain authoritative.
- Capability limits: this is a workspace recommendation, not a new per-user default, content conversion, or per-editor plugin-settings system. Usage is a current aggregate snapshot. New creation sessions receive the policy; already open sessions still encounter the server’s current creation checks at save time. No database migration is needed.

Validation: all 30 policy/formats/preview/review/chooser width-and-theme browser cases passed without overflow, JavaScript errors or WCAG A/AA violations. Interactions covered recommendation order, the last-editor guard, recommendation clearing, saved/draft preview, navigation protection, conflict recovery, save locking and read retry. Six isolated PostgreSQL tests passed atomic persistence, unrelated-setting preservation, concurrent-save rejection, database rollback, activation warnings and stale fingerprint rejection after an intervening change. Policy, template privacy/availability/access failure, transport, legacy site configuration, creation enforcement and editor-shell regressions passed. Shared/client/server type checks and repository lint passed. Production deployment and unintercepted policy restoration remain pending.


Editors deployment `951e3184` is healthy and the container revision matches committed source. The production Docker build and bundle gates passed. Unintercepted browser verification passed 30 creation-policy, formats, preview, review and real chooser cases across desktop/tablet/phone widths in both themes, without overflow, JavaScript errors or WCAG A/AA violations. A recommendation saved through the UI persisted and appeared first in a newly opened chooser; a stale policy write returned HTTP 409 and anonymous access returned HTTP 403. Original availability and recommendation were restored and read back. No pages were created or changed. `compose.before-951e3184.yml` retains the previous healthy image.

## Rendering initial findings

The current interface presents a deeply nested module directory with repeated colored toolbars. It does not distinguish drafts from saved configuration, guard navigation, explain inactive dependencies or show the effective path from source to HTML. Saves update module rows sequentially and accept unknown keys and invalid option values, so failures can leave a partial pipeline. The runtime supports four core source formats and ordered HTML post-processing; several modules produce remote media/diagram references, and image prefetch can perform server requests. An output inspector must distinguish stored output from a fresh render and keep untrusted page HTML isolated. The next implementation will provide a searchable module workspace, explicit dependency/pipeline inspection, atomic validated settings with stale-write protection, and permission-checked stored-output inspection with useful structure diagnostics. Existing content re-rendering will remain an explicit operation with clear effects.


### Rendering implementation and verification

- Replaced nested colored panels with directly linked Modules, Pipeline and Stored output sections. The module directory supports text, family, effective-state and changed-draft filters. Configuration has independent saved/draft state, per-module and whole-workspace reset, typed controls, visible validation, leave protection and a reviewed save summary. Enabled extensions whose core is disabled are explicitly paused.
- The administration trace and render worker share one format/dependency planner. It shows enabled core transformations, parser extensions and ordered HTML post-processors, with saved-versus-draft comparison and direct configuration links. Diagnostics distinguish missing parsers, bypassed HTML processing, disabled sanitization, embedded frames and competing math engines from observed service health. Modules with provider effects explain where diagram/media content can be requested.
- New administrator-only workspace API atomically persists validated module rows with PostgreSQL locking and fingerprint-based stale-write rejection. Fingerprints cover installed definitions and a persisted revision; intervening restorations and legacy writes invalidate reviews. Unknown stored options survive, while unknown incoming modules/options, invalid types/enums, invalid diagram endpoints/markers and unreasonable tab widths are rejected. Legacy REST/GraphQL updates use the same transactional store. No migration is required.
- New permission-checked stored-output inspection delegates page access and unlock policy to the existing page operation. It returns bounded HTML, current page metadata, headings and actual stored link markers. The interface offers an isolated structure preview, escaped HTML source, heading/marker inspection and a text export. DOMPurify, a resource-blocking CSP and an empty iframe sandbox keep authored scripts, navigation, forms and external requests out of the preview.
- Explicit single-page re-rendering uses the existing worker, explains provider effects and guards navigation while awaiting completion. Failed requests preserve an uncertain-outcome message and permit output inspection before retrying. A missing parser now fails the worker before it can replace stored output with untransformed source. Source revision fencing remains intact.
- Capability limits: pipeline tracing is a configuration projection, not an arbitrary draft render or provider-health test. Authoring previews use their own renderer and can differ from published output. Stored HTML does not record its rendering-configuration or source revision, so the inspector labels current source metadata without claiming output freshness. Inspection is limited to 2 MiB; the structure preview deliberately omits interactive extensions, page styles and remote assets. Page selection uses the existing accessible list; no durable re-render job history or estate-wide resumable job has been added here.

Validation: 54 module/pipeline/output/review responsive-theme cases and 18 paused/invalid/re-render-review cases passed without overflow, JavaScript errors or WCAG A/AA violations. The isolated preview made no requests from authored test URLs. Six PostgreSQL integration cases passed atomic multi-module persistence, rollback on a later write, simultaneous-save rejection, legacy/restore invalidation, strict configuration validation and installed-definition changes. Eleven targeted regression files passed shared planning, preview isolation, transports, private output access, render-worker revision/empty-pipeline behavior and actual component draft/lifecycle behavior. Shared/client/server type checks and repository lint passed. Interactions also passed library search, module-to-pipeline navigation/bookmark restoration, draft isolation and leave protection, conflict recovery, save locking/persistence, explicit re-rendering, late-output suppression and read retry. Production verification is the remaining gate.


Rendering deployment `29593ac0` is healthy and its container revision matches committed source. Production build and bundle gates passed. Unintercepted browser verification passed 54 module/pipeline/output/re-render-review width/theme cases with no overflow, JavaScript errors or WCAG A/AA violations. A UI-saved typographer change persisted, stale configuration returned HTTP 409, and an invalid enumerated option returned HTTP 400. A real worker re-render changed the expected punctuation in a temporary private page’s stored HTML while source, source revision, privacy, ownership and publication stayed unchanged. Original renderer configuration was restored and read back; the temporary page was deleted. Anonymous workspace and output inspection returned HTTP 403. No enabled webhook receivers were present and the fixture contained no external rendering inputs.

The first live attempt completed the worker but timed out waiting for the UI confirmation; its configuration and private fixture were also restored/removed. The response-captured repeat verified HTTP 200 and correct output. Further review found that an inspection failure could hide the separate worker-completion notice. That notice now remains visible independently of output loading/errors, with a component regression and controlled browser failure/retry check. Deployment verification of this final presentation fix remains pending. `compose.before-29593ac0.yml` retains the preceding healthy image.


## Comments initial findings

The current administration page only selects/configures a provider. It has no saved baseline, dirty gating, navigation protection, native-comment inventory or reversible moderation. Provider saves update rows sequentially and then activate an arbitrary enabled provider; native comments remain in the database when an external embed is selected. Akismet credentials are unmasked in the old configuration form, and the global discussion feature is controlled elsewhere in General. Native reads and mutations enforce page visibility and comment permissions but do not carry the page-password unlock context. Third-party embed templates also need explicit handling for private pages and validation/escaping of configured values.

The planned workspace will distinguish discussion policy, provider configuration and local moderation; provide a real searchable native-comment register with reversible hide/restore and history; support understandable per-page discussion closure where feasible; and make provider capabilities, retained local data, privacy boundaries and actual anti-spam behavior explicit. Policy persistence must preserve unrelated settings and support conflict/recovery, while reader APIs retain page access/unlock checks. External-provider moderation remains owned by that provider; the application must not fabricate its activity or verification status. Comments remains pending implementation.


Final Rendering image `ed9b4f27` is healthy and its revision matches the committed implementation. Production build/bundle gates, client type check, lint and the completion-versus-inspection component regression passed. Six additional responsive/theme cases verified the completion notice alongside a failed output inspection without overflow or WCAG A/AA violations. A direct real-worker request and three consecutive browser re-renders returned HTTP 200 in approximately 1.5 seconds each. The last browser run deliberately failed the following inspection response and verified that real worker completion remained visible and retry recovered the output. Earlier live-test timeouts were not reproduced in these instrumented repetitions. Original settings are restored; all five temporary private pages used across verification and timing diagnostics were removed. The isolated PostgreSQL test container and its temporary credential file were removed. Browser authentication state was removed after verification. `compose.before-ed9b4f27.yml` retains the preceding image.


### Comments implementation and verification

- Replaced the provider-only form with Discussion policy, Moderation register and Closed discussions. Provider selection has keyboard navigation, masked credential replacement/removal, a saved baseline, typed validation, reset, reviewed saves and navigation protection. Global discussion availability is controlled here and uses the same persistence path as General.
- Added server-paginated native-comment search across content, authors and page metadata, with comment/page visibility filters. Contextual review shows escaped Markdown source without executing or loading authored resources, reveals author audit fields on demand, and supports reversible hide/restore with an administrator-only reason and the latest 50 history entries. Hidden comments are excluded from reader list/single responses.
- Added per-page close/reopen policy with reasons, history, stale-review rejection and a searchable closed-page register. Readers retain visible comments but see a closure notice and cannot post. External services retain their own moderation/lifecycle controls; local history is not fabricated for them.
- Provider settings use transactional PostgreSQL persistence, exclusive-provider validation, masked-secret preservation, explicit removal, unrelated-feature preservation and revision fingerprints. Legacy REST/GraphQL provider updates delegate to the same store. Runtime activation is serialized and committed writes report activation warnings separately.
- Native posting applies persisted availability/provider/closure rules, validates reply parents, separates guest delays by IP, measures delay from creation time and serializes competing identity writes. Page, discussion and settings locks keep closure/pause boundaries coherent with an accepted in-flight post. The existing 15-second attempt limiter remains. Native comment operations retain local storage access after a provider switch and carry the authenticated session through page-password checks.
- External embeds are built with validated URLs/shortnames and context-safe encoding. Private/password-protected pages suppress all external embed code. Native Akismet requests skip those pages, use bounded timeouts, and do not use a superseded credential while activation is pending. The workspace distinguishes configured credentials from observed activation-time verification. Failed spam checks leave comments available for local moderation; no spam queue or external moderation status is invented.
- Migration 16 adds hidden-state metadata, page policies and action history. History stores action/reason/actor identifiers without comment source, email or IP, and survives subject deletion. Down migration refuses lifecycle data. A database backup is required before the first deployed migration; an older image alone does not undo moderation state.
- Verification: 17 isolated PostgreSQL cases passed for policy atomicity/rollback, secret preservation, stale/ABA reviews, moderation races, closure, guest pacing, password grants, external-content boundaries and migration protection. Thirteen focused regression files cover transports, provider activation races, private routing, reader helpers and client draft/recovery behavior. Browser fixtures passed 42 width/theme views and real UI interactions for validation, masked saves, conflict recovery, hide/restore, close/reopen and keyboard provider selection. No overflow, WCAG A/AA violations, JavaScript errors or authored outbound-resource requests remained. The first visual run exposed light-theme accent contrast and a conflicting global hidden class; both were corrected and the complete matrix passed again.
- Capability limits: the register covers local comments only; external provider activity and health remain with those services. Akismet verification is observed at activation, not continuous health monitoring. The comment review deliberately displays source instead of rendering untrusted embeds. Deployment and unintercepted live persistence verification remain pending.


Comments is deployed as `be074436`, healthy with revision `be0744361138e289cf47a16a52aa28fba7047d8a`. Migration 16 is applied. The verified pre-migration custom-format backup is retained locally at `backups/before-discussion-moderation-16-20260906T080345Z.dump`; `compose.before-be074436.yml` retains the preceding image, while `compose.before-e17775ad.yml` retains the pre-moderation image. Downgrade still requires respecting the migration’s lifecycle-data guard.

Unintercepted verification passed 36 discussion workspace/detail width/theme cases. Real UI policy saves, stale HTTP 409 rejection, hide/restore, closed-page posting rejection, reader closure notice and reopening passed. Source, source revision, private visibility, owner and publication remained unchanged. Six additional live width/theme checks passed for General’s new link to the discussion workspace, which replaces its duplicate availability toggle and omits that field from unrelated General saves. Final route synchronization, the original paused policy, zero local comments and zero closed-page policies were read back. All three temporary private pages (32, 33, 34) and their comments were removed. Synthetic action metadata remains in the history table by design. No enabled webhook receivers were present; fixtures contained no external rendering inputs. The isolated PostgreSQL container and its credential file were removed.

Live verification exposed two additional issues that were corrected before the final pass: legacy comment exceptions lacked HTTP status and caused unavailable resources to return 500; and route-fragment changes could leave the selected panel stale. Both have regressions. The first navigation timeout was investigated with a direct route-fragment reproduction; the corrected build completed the complete live matrix and workflows. Focused client/server checks, lint and all four production image builds passed. The broader General overhaul remains pending despite its small integration adjustment here.

## Users initial findings

The user inventory already supports server pagination and identity-provider search, but has little lifecycle/access context. The large detail page mixes editable profile popovers, group assignment, welcome email, password and two-factor controls with immediate activate/deactivate/verify/delete actions. Profile/group writes use a database transaction but have no reviewed version fingerprint; lifecycle operations have separate protections and some requester constraints currently live only in the UI. Private-page ownership/history can block deletion, while public-content replacement is a separate destructive workflow.

The Users overhaul should provide a coherent directory and contextual account workspace, explicit saved/draft behavior, understandable provider capabilities and membership effects, and carefully reviewed account/security actions. Assess persisted lifecycle history, conflict protection, self/system-account guards and session revocation visibility alongside existing group-assignment authorization. Preserve existing access and private ownership. Welcome-email tests must not send messages; use explicit `sendWelcomeEmail: false` for synthetic accounts. Invitations and provider verification must be presented according to real supported behavior, not fabricated delivery or health state. Users remains pending implementation.


### Users implementation and verification

- Rebuilt the directory around people, memberships and account state. Server pagination, literal case-insensitive search, provider/group filters and active/inactive/unverified counts are backed by persisted records. Filter URLs survive account drill-down and return navigation. Nested administration routes now retain the owning sidebar highlight.
- Account workspaces separate Profile & membership, Sign-in & security and Activity. Independent profile drafts support clearable fields, reset, reviewed differences, navigation protection, stale-review recovery and delayed-response suppression. Provider capabilities determine password/authenticator controls; timestamps distinguish sign-in or renewal from live presence. Contribution counts describe current attribution, without implying a complete activity history.
- Creation has a provider-aware form, combined group-permission preview, a separate review and actual persisted identity return. Local administrative passwords require at least 12 characters and no more than bcrypt’s 72 UTF-8 bytes. Creation does not implicitly send mail. External accounts remain authenticated by their provider. Email ownership is an explicit administrator decision; this form does not send a verification link.
- Added reviewed activation/deactivation, email verification, authenticator enrollment/reset/removal, password replacement, session termination and account deletion. Current requester and current target group authority are resolved from persisted data. Self, Guest and primary/system-account protections apply at the backend. Membership or email changes end prior sessions; an email change clears verification. Deletion retains private ownership boundaries, refuses accounts owning private pages/history, and reassigns existing attribution while preserving other owners’ private content.
- Transactional account/group locks, revision fingerprints and audit metadata cover the new workflows and legacy REST/GraphQL adapters. Reviews include group/provider policy, reject competing or ABA edits, and commit state/history together. Reasons and changed field names are recorded without passwords, authenticator secrets or email bodies. Welcome email has an explicit review and stored requested/accepted/failed outcome; the reviewed address is checked before sending. Mail acceptance is not delivery confirmation.
- Migration 17 adds persisted account session versions, administrative revisions, session-end timestamps, recovery-key versions and administration history. Legacy sessions/keys start at version zero. HTTP authentication checks persisted account availability/version on every signed-in request. Expired JWT renewal verifies the signature, issuer and audience and carries the original generation into renewal. Password recovery/mandatory-change flows advance versions atomically. Recovery and continuation key issuance/consumption are version-bound and use account-before-key lock ordering.
- Existing group membership/permission changes now advance affected account generations. Collaboration tokens also carry the generation: live clients are rechecked on notifications and before incoming updates. GraphQL subscriptions revalidate the account through their existing authentication boundary. API-key lifecycles remain separate.
- Verification so far: 21 isolated PostgreSQL scenarios pass creation/deletion, concurrency, privilege boundaries, profile rollback, credential replacement, stale/ABA review, mail outcome metadata, pending-key invalidation, group authority and guarded migration up/down. Eighteen targeted regression files pass across transports, model transactions, token authority, subscriptions, real collaboration sockets and client draft/recovery behavior. Browser fixtures pass 55 responsive/theme views with no document overflow, WCAG A/AA violations or JavaScript errors, plus actual creation submission, conflict recovery, locked saves, lifecycle actions, welcome-audit presentation, keyboard navigation, filtered return navigation and leave protection. No real email was sent. Shared/server/client checks and lint passed; production build/deployment and unintercepted workflows remain pending.
- Capability limits: there is no per-device session registry or fabricated device count. Pending recovery/sign-in links also become invalid when their account generation advances. External-provider session state and verification remain with that provider. The account activity panel shows the latest 50 recorded administrative events; older pre-migration actions are not reconstructed, and retained events for deleted subjects are not exposed through a separate global audit browser. The new 12-character administrative password minimum does not yet replace all legacy registration/self-service password policies; that wider policy belongs to Security. No invitation expiry/resend queue or email-delivery tracking is claimed.

A verified database backup is required before the first migration-17 deployment. An older container image alone must not undo persisted revocation or lifecycle history; the down migration refuses such state.

Pre-migration archive verified: `backups/before-account-administration-17-20260906T094735Z.dump` (2,901,868 bytes; 609 archive entries). It includes the deployed discussion lifecycle and precedes account migration 17.


Users is deployed as `5b4bac50`, healthy with revision `5b4bac503911afaed29d15ace94c64c836f78861`. Migration 17 and all three account columns were read back from PostgreSQL. The production Docker build and bundle gates passed. `compose.before-5b4bac50.yml` retains the preceding image; the verified pre-migration archive remains available. Restoring an older image alone does not reverse account generations or recorded lifecycle state.

Unintercepted verification passed 42 width/theme views across the directory, creation, profile, security, password review, deletion review and activity. The real UI created an account and cleared optional profile fields through reviewed saves. Real sign-ins verified that ending sessions invalidates existing JWTs, reactivation does not revive a revoked token, password replacement invalidates the previous session, and the new password can sign in. Stale profile reviews returned HTTP 409. Primary and Guest deactivation were rejected. A delegated account manager could not change the primary administrator’s profile or password. Changing the temporary group’s permissions invalidated its member’s prior sign-in. Private pages and retained private history blocked account deletion.

The browser matrix had no document overflow, WCAG A/AA violations or JavaScript errors. A first cold browser load timed out before the app appeared; three subsequent fresh contexts rendered the directory in 533, 968 and 500 milliseconds with no failed requests or JavaScript errors. Eight authenticated `whoami` requests took approximately 3 milliseconds each on the local deployment; these are observations, not a load benchmark.

All three temporary accounts (13–15), two groups (5–6), and private pages (35–36) were removed. The initial ownership test correctly refused account deletion after page deletion because private history was retained; the harness was corrected to expect HTTP 403 and explicitly removed only those two synthetic page-history records before deleting their temporary owners. Original accounts and group memberships remain unchanged. Administrative action metadata remains by design. No real email was sent; no enabled webhook receivers or external rendering inputs were used. The isolated PostgreSQL container and temporary credential/authentication-state files were removed after verification.

## Groups initial findings

The group directory emphasizes numeric IDs and timestamps rather than access purpose or policy breadth. Details split settings, permissions, page rules and membership into legacy panels; policy changes are staged, while member assignment/removal takes effect immediately. The update action lacks a reviewed policy version, so membership and policy changes are difficult to reason about together. Page-rule editing exposes low-level matching controls without an effective-access explanation. The Users milestone now makes existing group changes invalidate affected sessions, but does not complete the Groups workflow.

The next overhaul should make group purpose and scope understandable, separate saved policy from drafts, review permission and membership effects, protect system groups and current administrative authority, and add a useful access evaluation grounded in the actual authorization rules. Private ownership must remain a separate boundary, and any policy preview must state whether it evaluates this group or a person’s combined memberships. Group creation, deletion, membership, page-rule management and recovery states all remain in scope.


### Groups backend checkpoint — interface and deployment pending

The new group administration store supplies a purpose-aware directory, paginated membership/candidate lookup, reviewed creation/policy/membership/deletion operations, and saved or draft access evaluation for a group or a current member’s combined memberships. The directory omits full rule bodies; access reports return at most 200 rule explanations with an explicit total/truncation indicator. Evaluation describes a hypothetical public-page policy decision. Private ownership, authentication availability, provider grants, agent tool approvals and other feature-specific gates remain separate.

The actual request authorization path and the evaluator now share the same page-rule engine. Focused tests preserve longer-text precedence, match-type precedence, deny ties, language/action exclusions, regex and suffix matching, cross-group decisions and taxonomy aliases/archive state. Normal request checks skip explanation allocation. Administrative reads use PostgreSQL read-only repeatable-read transactions; writes lock group and affected account rows in a consistent order, check current persisted actor/target authority, validate reviewed fingerprints, and commit state, session generations and history together. Fingerprints include member account versions to reject account-side membership ABA changes. Legacy REST/GraphQL writes delegate to the same store; their transports still lack client review tokens and therefore obtain their saved version at the adapter boundary.

Migration 18 adds group purpose, administrative revisions and retained group history. Group-driven membership and policy changes also appear in affected account history. Existing active API credentials, authentication enrollment references, navigation audiences, and agent provider/skill grants are reported and block deletion until resolved. The deletion/invalidation integration still needs review alongside the final UI, including concurrent external configuration writers and notification failures; this checkpoint is not a completed Groups milestone.

Sixteen focused policy/PostgreSQL scenarios and the 21 adjacent account PostgreSQL scenarios pass. Tests cover creation, clearable purpose, permission effects, current authority, protected identities, concurrent/ABA edits, rollback, both histories, dependency guards, credential redaction, Guest visibility and migration guards. Shared/server/client type checks and lint passed. The REST/GraphQL adapter, new workspace transport, migration and authorization regressions pass. The tests caught and corrected a comparison bug where JSON property ordering could make a purpose-only edit end sessions. The original live directory was inspected and captured; its visual/workflow replacement has not yet been implemented or deployed.

Next: build the directory and creation flow, then the complete group workspace (overview/purpose, permissions, page rules, members, access check and activity). Add isolated drafts, reviewed differences, clear conflict/retry outcomes, save/navigation locking, dependency links and appropriate system-group explanations. Verify the actual workflows, mobile/tablet/desktop and both themes; perform a fresh production build, pre-migration backup and Docker deployment only after the complete Groups milestone is ready. The current deployment remains the verified Users image `5b4bac50` with migration 17.

### Groups complete interface — deployment verification pending

The directory now organizes groups by purpose and access breadth, with administrative/system/empty filters, search, pagination and return-to-filter navigation. Creation offers explicit starting policies, defaults to no access, requires a reviewed administrative reason and never silently adds members or credentials. Each group has six directly addressable sections: identity/purpose and dependencies, human-readable permissions, page rules, membership, access checks and retained activity. The presentation uses restrained record rows, a readable permission catalog, clear typography and a single policy review action shared across draft sections.

Rules and policy changes remain isolated drafts until reviewed. The review captures a fixed policy or member selection, shows differences and requires a reason. Membership changes are unavailable while policy drafts exist. Busy writes lock navigation; unsaved policy/rule/reason drafts are protected; conflicting or uncertain outcomes retain the draft and require reload and re-review. A successful write remains visibly successful if its follow-up read fails. Deletion requires an exact group ID and resolved dependencies. System identities and accounts outside the administrator’s authority have explicit explanations.

The access workspace evaluates either this group or a current member’s combined memberships, with saved/draft source, hypothetical page path/language/tags, actual rule precedence, exclusions, and stale-result notices. It does not claim to simulate private ownership or every feature-specific gate. Membership rows link to account administration when authorized and can open that person’s combined access check.

Browser fixture verification passed 78 view/theme/size combinations (1440, 900 and 390 pixels; light and dark), including all sections and creation/rule/review/deletion dialogs. No horizontal overflow, detected WCAG AA violations, JavaScript errors or unintended live mutations occurred. Interactions covered creation, policy conflict/recovery, fixed saves with navigation locked, permission and rule drafts, reviewed member additions/removals, saved/draft/member access evaluation, keyboard tabs, unsaved navigation, query-preserving filters, empty/error recovery and dependency-aware deletion. Visual inspection prompted flatter permission rows and corrected access-form label spacing. These are preview fixtures; live persisted workflows remain a separate deployment gate.

Nineteen focused frontend behavior cases replace six obsolete source-string migration guards. Seventeen policy/PostgreSQL scenarios pass, including a new notification-failure test proving state/history/session generations commit and account revocation continues despite failed cache/peer delivery. The authorization cache now clears stale group policy and expires Guest state when refresh fails; recovery and adjacent page/API/subscription authorization tests pass. Existing 21 account PostgreSQL scenarios also passed for the shared backend checkpoint.

Integration boundary: deletion checks committed enrollment/navigation/credential references, and agent grants retain database foreign keys. Legacy Authentication and Navigation writers still store group references in JSON without a shared reference-validation transaction. Concurrent stale edits in those areas can recreate dangling references after deletion; they do not recreate the deleted group or its permissions. Their forthcoming overhauls must validate current group identities transactionally. Distributed cache notifications also retain the existing best-effort event transport; this deployment has one application container. No cross-node immediate-revocation guarantee is claimed.

The Groups source is ready for final static/build checks, a fresh pre-migration-18 database backup and deployment verification. The running production image remains `5b4bac50` until that gate completes.

### Groups deployment and live verification

Deployed `tsepistle:7f2769c8` (revision `7f2769c8a418e59c17b1999a3725c04e66502036`) to the local Docker service; health and migration 18 are verified. The pre-migration backup is `before-group-administration-18-20260906T112615Z.dump` (2,907,825 bytes, 602 verified archive entries, mode 600). `compose.before-7f2769c8.yml` preserves the prior Users deployment configuration. Restoring the old image alone does not undo migration 18 or its recorded administration history.

Unintercepted live browser verification passed 48 views across desktop/tablet/mobile and both themes, with no detected WCAG AA violations, horizontal overflow or JavaScript errors. Actual UI workflows created a group with no implicit access, cleared its purpose, granted a page permission with a scoped path rule, compared allowed/denied paths, added a reviewed member, evaluated that member’s combined policy and compared an unsaved draft with saved access. The session existing before membership assignment was invalidated. A freshly signed-in delegated content-group administrator could inspect the group but could not grant system authority; root and Guest deletion were rejected. Deleting the temporary group through the reviewed UI ended the member’s refreshed session.

Temporary groups 7–8 and account 16 were removed. Original groups 1–3 and the original account population are preserved; the group directory exactly matches its baseline. No email was sent and no content fixtures were created. Retained administrative events accurately record these synthetic changes. Final client/server type checks, lint, focused frontend and authorization tests, Vite build and bundle budgets passed before the release build. This is the twelfth verified subsection milestone; Authentication, Security and the remaining workspace/operations areas are still pending under the active overall goal.

## Authentication initial findings and implementation direction

The live `/a/auth` baseline has one Local provider. Desktop/mobile captures show a nested provider sidebar, a long undifferentiated form, weak disabled-state readability, legacy provider branding, and no clear separation between sign-in configuration, registration policy, runtime initialization or the end-user login experience. The Local provider’s protection exists in client controls but the bulk update operation does not enforce the same invariant. Adding a provider immediately stages it enabled. Callback/reference information is generic even though form, redirect and SAML integrations have different setup needs. Secrets are masked by the existing serialization layer; retain that boundary.

The existing `updateStrategies` writes each provider independently before checking whether removed providers still own accounts. It has no saved-version review, transaction-wide rollback, current persisted administrator recheck, retained administrative reason/history, or enrollment-group reference validation. Runtime initialization clears the strategy registry and catches failures internally, so successful persistence is not proof a sign-in method initialized. It also attempts initialization for disabled records. The UI aborts requests on navigation without resolving uncertain save outcomes and lacks an unsaved route guard.

Provisioning deserves an explicit workflow: domain restrictions, initial groups, account counts, and directory claim mapping have different effects. Local registration currently treats direct-array domain/enrollment values as empty despite the model supporting both direct arrays and wrapped arrays. LDAP, OIDC, SAML and Azure mapping replace memberships through individual relation writes using cached name matches; they do not share the reviewed group/account transaction or session-generation history. Provider disablement does not itself advance existing account session generations. These behaviors must be assessed and corrected as part of this area, with live external-provider checks kept distinct from static configuration checks. No external sign-in flow or email was triggered during this audit.

Planned composition: a provider inventory with availability, account/admission context and observed runtime status; a searchable add-provider catalog that starts disabled; a focused provider workspace with connection fields, provisioning, integration values and retained activity; a separate login-order preview; and a reviewed save workflow showing changed secrets by field name only, admission changes, account/session effects and dependencies. Preserve the built-in local recovery route server-side. Implement current authority and fingerprints, atomic provider/enrollment writes coordinated with Groups locks, bounded redacted history, explicit activation outcomes and recovery. Validate actual form defaults/types against provider definitions, preserve unknown stored values when appropriate, and avoid presenting configuration validation as a successful remote login. Tests must cover rollback, concurrent edits, protected local login, secret preservation/redaction, current enrollment references, account/session effects and failure recovery before migration/deployment.

### Authentication foundation checkpoint — interface, legacy adapters and deployment pending

Added a typed authentication workspace and explicit keep/replace/clear secret actions. New `GET/PUT /_api/auth/admin/workspace` operations require system authority at the transport and recheck persisted authority inside the store. The store reads a consistent snapshot and saves the full reviewed provider list atomically, with server-keyed fingerprints covering providers, account usage and group access. It validates Local recovery, unique provider identities, immutable provider types, field types/enums, exact normalized domains and current enrollment groups. System identities and script authority cannot be granted through automatic enrollment. Unknown existing configuration is retained without exposing it; known secrets are represented by configured state and actions. History records changed field names, never secret values.

Provider configuration/enablement changes advance affected account session generations and record account history in the same transaction. Display, purpose and new-account admission changes do not end existing sessions. Deletion checks account references before writes. Enrollment writes lock Groups before providers/accounts, so a concurrent group deletion invalidates the reviewed snapshot instead of creating a dangling reference. Post-commit activation failure returns saved-state success with an explicit needs-attention outcome.

The runtime now records pending/ready/failed/disabled initialization observations against each saved revision, skips disabled modules and serializes reloads to prevent older initialization from overtaking newer configuration. A partially registered failing provider is removed. Explicit provider audience configuration is preserved rather than overwritten by the workspace audience. Shared plugin objects are no longer mutated with instance-specific configuration. A missing on-disk definition retains its database record for administration rather than attempting automatic deletion. These observations describe local initialization, not remote identity-provider connectivity or successful end-user sign-in.

Migration 19 adds provider purpose/revisions and retained authentication history, with a rollback guard once history or edits exist. Fifteen focused policy/PostgreSQL cases pass, including rollback, secret redaction/preservation/clearing, current authority, Local protection, admission restrictions, deletion dependencies, concurrent reviews, concurrent group deletion, session effects, post-commit activation failure and migration safety. Four runtime and four new HTTP-boundary cases pass. Existing authentication HTTP, migration contract, account-session, subscription, API authority, provider-definition and sign-in-controller regressions pass. Shared/server/client type checks and lint pass.

This is a source checkpoint, not a completed Authentication subsection. The live container remains the verified Groups image `7f2769c8` with migration 18. Before deploying migration 19: route the legacy REST configuration write through the reviewed store (the current GraphQL schema has no provider-configuration mutation); complete registration and directory-mapping/session consistency fixes; build the provider inventory, catalog, focused editor, provisioning/integration views, login-order preview, review/recovery states and activation retry; verify actual UI behavior, both themes and all screen sizes; and perform a fresh backup/release/deployment cycle. The isolated PostgreSQL fixture service `tsepistle-auth-test` is reserved for continuing these tests; it contains synthetic data only.


### Authentication complete workspace — deployment verification pending

The provider inventory now shows purpose, admission, account usage and locally observed initialization. A searchable catalog starts new connections disabled. Directly addressable Connection, Enrollment and Integration views organize service settings, profile claims, directory mapping and collapsed advanced protocol options. Credentials have explicit keep/replace/clear actions. The Login order workspace stages keyboard-accessible reordering beside an enabled-method preview. Activity retains the latest 50 reviewed policy events, including administrative reasons and session effects.

A single fixed review covers creation, edits, removal, enrollment and order. Admission reviews show domain/group values; secrets appear only by field name and action. Busy writes prevent navigation, dirty drafts prompt before leaving, and conflicts or uncertain outcomes require reload before another save. Committed creation/deletion remains correctly represented if the following read fails, and replacement credential inputs are cleared after success. Clean saved policies can retry initialization through a system-authorized, fingerprint-checked endpoint. Initialization remains distinct from remote connectivity and real sign-in validation.

The legacy REST configuration writer now delegates to the same current-authority, transactional store. It receives its review fingerprint at the adapter boundary because the older transport has no client review token. Inspection confirmed that the current GraphQL schema exposes provider reads but no provider-configuration mutation; no speculative mutation was added. New-account provisioning reads current provider admission and group references under the same lock order before inserting an account. Local registration supports direct and wrapped enrollment values and writes the actual provider/locale foreign-key columns. JWT issuance rejects a disabled provider.

LDAP, OIDC, SAML and Azure group mapping now share a transactional synchronizer using current persisted provider configuration and group names. This preserves the existing explicit name-mapping contract: a missing claim leaves memberships untouched; a present empty claim removes them; present names replace memberships with matching workspace groups, including administrative groups. The interface explains this consequence. Changed memberships, account session generations, account history and group history commit together; failed audit insertion rolls everything back. Protocol adapters refresh the returned account version and discard cached group claims before token issuance. Local and peer revocation notifications remain best-effort after the durable transaction.

Twenty PostgreSQL/policy scenarios pass, including both enrollment encodings, current provider protection, directory mapping idempotence and audit rollback, initialization retry authority/review, plus the previously verified secret, concurrency and migration cases. Twelve protocol-adapter cases cover all four modules, missing versus empty claims, fresh versions and failure propagation. Five workspace transport and eight frontend behavior cases pass alongside legacy authentication, registration and session tests. The browser fixture matrix covers 60 desktop/tablet/mobile and light/dark views; credential review/conflict recovery, purpose clearing, advanced/mapping controls, disabled creation, removal, order, initialization retry and navigation workflows pass. These preview checks are separate from the required unintercepted deployment verification.

Migration 19 is still pending on the local deployment at this checkpoint. Release requires the final build/checks, fresh verified backup, Docker replacement, health/revision/migration checks and reversible live provider workflows. No external provider login or actual registration email has been triggered.


### Authentication deployment verification

Deployed `tsepistle:0321e529` (revision `0321e529bb9a9fac2360bb89df0654924bdd85f2`) with healthy status and verified migration 19. The fresh database backup is `before-authentication-administration-19-20260906T123346Z.dump` (2,911,388 bytes; 610 verified archive entries; mode 600). `compose.before-0321e529.yml` preserves the previous Groups image configuration. Returning to that older image alone does not undo migration 19 or retained policy history.

Unintercepted live verification passed 54 desktop/tablet/mobile and light/dark views covering the inventory, catalog, order, activity, Local/provider connection, enrollment, integration and review. No detected WCAG AA violations, document overflow or JavaScript errors occurred. Actual reviewed UI saves verified purpose persistence/clearing, disabled LDAP creation, credential storage/redaction/explicit clearing, normalized email-domain enrollment, ordering, initialization retry and provider removal. Stale writes returned HTTP 409 and attempted Local disablement returned HTTP 400. Anonymous administration access was denied. The disabled connection never appeared in public sign-in choices.

Temporary provider `fbf1d3b9-d103-4ddd-b8bd-bcc224ee4a7d` was removed through the UI. Original provider configuration and account population are preserved; policy action metadata remains by design. No external identity-provider request, real registration email or content fixture was used. A final integration audit found automatic login selecting the first database provider regardless of enablement; a focused follow-up filters enabled providers and handles an empty result while preserving the all-methods recovery link. This server-only follow-up is awaiting its release before Authentication is marked complete.


Authentication follow-up `tsepistle:0a06174a` (revision `0a06174aff8ffc7ef9cd42899ca7c5ff0cd417c7`) is deployed and healthy with migration 19. The focused controller regression passes, and a real anonymous `/login` request with automatic login enabled and a disabled OIDC provider first in the saved order correctly rendered Local sign-in. `/login?all=1` also remained available. The temporary OIDC provider and temporary automatic-login setting were restored. `compose.before-0a06174a.yml` retains the preceding Authentication image. This completes the thirteenth verified subsection milestone. External identity-provider round trips remain untested because this deployment has only Local sign-in configured; initialization is accurately labeled rather than represented as remote connectivity.

## Security initial findings and implementation direction

The current page combines general HTTP protections, upload limits, login artwork/visibility, two-factor requirements and raw JWT values in unrelated cards. It uses the broad legacy site-configuration writer, which mutates the live object before persistence, saves each top-level settings row separately, and can overwrite unrelated settings from stale in-memory state. There is no reviewed policy fingerprint, reason/history, durable session-effect review or clear saved/runtime distinction. Server validation is considerably weaker than the form, and hidden SRI/CSP settings are still copied through saves.

Runtime inspection found several concrete mismatches to address. Upload byte/batch limits are captured when Multer is constructed, so changing the settings does not update those limits live. The upload controller accepts one file per request even though the legacy screen advertises a batch limit. SVG sanitization and attachment delivery are actual runtime features. The SRI flag has no enforcing consumer. CSP can emit an enforced header, but its controls are commented out and it has no staged report-only workflow. HSTS always includes subdomains, which the new review must explain or make explicit. Proxy trust is a one-hop boolean requiring a deployment-specific explanation. The open-redirect toggle only collapses repeated slashes in the request URL and should not be presented as a universal redirect protection guarantee.

Token expiration and expired-token renewal are different controls: the current renewal period is a grace window after token expiry, not a maximum session lifetime. Changing the JWT audience also requires authentication-runtime refresh, which the site writer does not currently perform. Two-factor enforcement applies to form-based provider flows; redirect providers delegate their factors to the identity service. Administrative passwords require 12 characters and at most 72 UTF-8 bytes, but registration and several reset/self-service routes still accept six characters. The overhaul should unify new-password enforcement and explain which existing sessions are ended by changed sign-in policy.

Proposed workspace: Access & sessions (password policy, form-provider two-factor, token lifetime/renewal, actual account coverage and reviewed session effects), Browser & transport (headers, HTTPS/proxy context, optional staged CSP with observed versus configured state), Files (effective upload limits, SVG handling and attachment delivery), Sign-in experience (automatic routing, Local visibility/recovery and background preview), and retained policy Activity. Link to Authentication and Users for provider/per-account work. Implement a narrow, transactional policy store that rechecks current authority, rejects stale reviews, preserves unrelated configuration and coordinates account generations/history; adapt legacy security writes so the old transport cannot bypass those safeguards. Provide precise runtime diagnostics and recovery without invented per-device session counts or remote security certification. These are audited next steps, not completed Security features.

Authentication cleanup is complete: the isolated PostgreSQL fixture container and temporary database/browser-state credential files were removed. Desktop/mobile captures of the current live Security screen are retained as its baseline. The overall goal remains active with 13 of 27 areas verified; Security and the remaining workspace/operations areas are unfinished.


### Security persistence and runtime checkpoint — not deployed

The previous goal turn made verified progress: Authentication was released, exercised against the real service, restored to its original provider policy, and recorded as subsection 13. This turn advances Security with a narrow typed policy, retained review history and actual runtime integrations. The full 27-area objective remains unchanged.

Added `GET/PUT /_api/site/security` and `POST /_api/site/security/activate`. Transport checks and persisted group/account authority protect each operation. Reads use a consistent read-only snapshot. Saves lock groups, providers, account generations and policy settings in identity-administration order, reject stale server-keyed fingerprints, preserve unrelated persisted settings, and commit configuration, session generations and both security/account history together. Token audience/lifetime/renewal changes and newly required workspace two-factor authentication end existing account generations; administrators may also explicitly end sessions. Guest remains separate. Failed post-commit runtime activation reports saved-state attention and can be retried against a fresh authorized review.

The policy separates token lifetime from the expired-token renewal window, validates bounded typed inputs, introduces a Unicode/UTF-8-aware new-password rule, and adds staged CSP mode plus explicit HSTS subdomain scope. New CSP directives must first be saved report-only before enforcement. The header middleware emits the selected report-only/enforced header and honors the HSTS scope. The Local-visibility setting requires another enabled, available provider. HTTPS browser memory requires an HTTPS configured workspace address. These configuration guards are not claims of remote security validation or successful provider login.

Legacy security writes now delegate to the same store and obtain their fingerprint at the transport adapter boundary. Requests mixing security and unrelated workspace settings are rejected so a security transaction cannot partly succeed inside a broader legacy save. General site writes no longer rewrite authentication/security/uploads rows. Unchanged legacy batch/SRI values are tolerated for old clients, while attempts to change the obsolete multipart batch setting or nonfunctional SRI toggle are rejected explicitly. Legacy clients that cannot express CSP report-only mode retain that mode when saving unrelated settings. The current GraphQL schema still exposes site configuration reads but no site configuration mutation.

Upload limits are captured for each new request, rather than at startup. A zero maximum explicitly disables uploads. Oversized files receive HTTP 413 with an understandable message after partial-file cleanup. The single-file request contract is preserved. JWT runtime initialization now records its observed audience; policy activation reinitializes authentication only when that audience differs. Config-reload events apply the same comparison, and failed JWT registration clears the observation. Other policy edits therefore avoid unnecessary identity-provider initialization. This retains the existing best-effort cross-node event transport rather than claiming guaranteed immediate distributed delivery.

Thirteen isolated PostgreSQL/projection cases and six shared-policy cases pass. They cover current authority, coverage/secret redaction, unrelated-setting preservation, exact session effects, explicit session-only actions, CSP staging, Local/HTTPS guards, concurrent and stale saves, transaction rollback, post-commit failure/retry, real legacy conversion, runtime audience activation and migration reversal. Four new HTTP-boundary cases pass, alongside site/logo, upload, middleware, migration, JWT-runtime, config-reload and account/API/subscription regressions. Shared/server/client type checks and lint pass. The upload integration tests use real multipart HTTP requests and verify changed limits without rebuilding the controller.

Migration 20 adds security history and its reviewed revision marker, but has not been deployed. The shared password validator is not yet connected to every password-creation/reset/self-service route; that is required before release. The complete Access & sessions, Browser & transport, Files, Sign-in experience and Activity interface, immutable reviews/recovery, diagnostics, responsive/theme verification, migration backup, Docker release and unintercepted live workflow checks are also pending. Do not treat this source checkpoint as a completed Security subsection or deploy it as the finished milestone. The local service remains healthy on `0a06174a`, migration 19. The isolated `tsepistle-security-test` PostgreSQL container on port 55438 is reserved for continuing these synthetic tests.


## Security interface and password-policy checkpoint

The Security workspace now separates Access & sessions, Browser & transport, Files, Sign-in experience and Activity. The interface exposes actual account coverage, token lifetime/renewal, reviewed global session ending, staged browser policy, observed response headers, per-file upload capacity (including disabled uploads), sign-in routing and background preview/asset selection. A saved-runtime sidebar distinguishes persisted configuration from observed activation, with retry and links to related access controls. Five sections use a shared editorial composition while retaining distinct workflows.

Changes require an immutable before/after review and administrative reason. Drafts survive conflicts and uncertain write outcomes; repeat submission locks until reload. Navigation guards, reset, stale-response suppression, committed-save/read-failure recovery and current-session ending are covered with behavioral tests. No per-device session registry or remote-provider health is fabricated. History shows the latest 50 recorded policy changes. CSP report-only requires administrators to supply directives and any desired reporting destination; this work does not introduce a report collector.

The saved minimum now applies transactionally to new Local passwords in administrative account creation/replacement, registration, mandatory change, password recovery and profile changes. A public, uncached requirements endpoint supplies matching form hints. Existing credentials remain valid; importing an existing legacy bcrypt credential preserves it. Initial setup uses the shared 12-character floor. All new plaintext credentials enforce the bcrypt 72-byte limit, and recovery/setup/profile paths hash bcrypt-shaped literal input instead of treating it as an already encoded credential. A PostgreSQL contention test confirms that a waiting password operation observes a newly committed policy minimum.

Verification: 14 real PostgreSQL Security/projection cases and 22 account cases passed; focused HTTP, Users-model, Security/Users UI and setup tests passed. Client/server type checks, lint, Vite and bundle budgets passed. Preview review covered 42 section/review combinations at 1440/900/390 px in light/dark with no tested WCAG A/AA violations or page overflow. Four additional light/dark desktop/mobile asset-picker views passed after correcting its FilePond attribution contrast. Browser flows covered conflict/reload, fractional MiB save, runtime retry, explicit background preview, asset-picker cancellation and dirty navigation protection, with no unexpected writes or browser exceptions. These preview writes were intercepted; this checkpoint is not live verification. Migration 20 backup, Docker deployment and unintercepted verification remain next.


### Security deployed verification

Release `ad7c9186a700cf131b753c140271f535fd1e142f` is running healthy in `wiki-tailnet`; migrations include `tsepistle-000020-security-administration.js`. The pre-20 archive `/home/bbferko/.local/state/wiki-tailnet/backups/before-security-administration-20-20260906T134415Z.dump` is 2,915,167 bytes, mode 600, with a readable restore listing. `compose.before-ad7c9186.yml` retains the preceding image reference; downgrading after recorded policy changes requires database restoration rather than an application-only rollback.

Unintercepted browser verification passed 42 section/review views at 1440/900/390 px in both themes, with no tested WCAG A/AA violations, overflow or JavaScript exceptions. The first launch immediately after deployment timed out on a blank page; a diagnostic load and complete rerun passed without source/configuration changes. A real reviewed upload-limit save persisted and activated; stale review returned 409. Raising the temporary password minimum to 16 updated the public uncached endpoint and account-creation hint. Short account creation and replacement were rejected, a valid replacement succeeded, and the temporary account signed in with the new password. No email was sent. Global session ending was reviewed only on the live instance; transactional revocation and other disruptive policy effects remain covered by isolated database/runtime tests.

Temporary account 17 was removed and the original account directory exactly matched its baseline. The full original Security policy was restored, with zero sessions ended by every policy save and observed runtime applied. Administrative history retains these truthful verification reasons. Security is the fourteenth verified subsection milestone; General and the remaining workspace/operations areas continue under the active goal.


## General findings and supporting implementation checkpoint

The existing form mixes identity, SEO, a logo publishing pipeline, announcements, page routing and edit shortcuts in mismatched columns. Its save payload includes Analytics values and feature flags without corresponding controls, and it lacks reset, draft navigation protection, review and stale-write recovery. The legacy server mutates runtime configuration before a sequential settings save, so failures can leave partial durable/runtime changes. External source links accept unvalidated URL templates. Ratings and personal-wiki feature flags have no corresponding runtime consumers in the current source and should not become decorative switches.

The intended General workspace separates Identity, Announcement, Publishing and Activity. Identity includes the existing managed-logo processing/retry workflow as an explicitly separate publication action, plus footer and metadata previews. Announcement gains tone and a start/end publication window with a preview that can show a scheduled draft. Publishing makes page extension aliases and editing actions concrete with example URLs and reader controls. Related capability links lead to dedicated Editors, Comments, Theme, Navigation and Analytics workspaces. Atomic reviewed settings, reset, conflict recovery, dirty navigation and saved-runtime observation underpin these workflows.

Implemented supporting source (not deployed): a typed General policy and strict validation for public origins, metadata, license identifiers, extensions, edit controls and HTTP(S) external source templates; a current-authority PostgreSQL store with locked atomic settings writes, HMAC review fingerprints, ABA protection, a bounded latest-50 revision history in the existing settings table, and activation retry. It preserves unrelated SEO/shortcut properties, authentication and feature settings. New system-only uncached General read/save/activate routes redact unexpected errors. There is no new migration. Legacy site-config writes still need to be routed through the reviewed boundary before release.

Optional announcement tone/start/end fields retain compatibility with existing banners. Shared publication logic withholds scheduled/ended content from reader bootstrap; an already open announcement expires at its end and rechecks after document suspension. Publication starts on the next reader document load; this does not introduce push delivery. The reader's external source link now only renders a valid HTTP(S) destination and safely encodes the page filename. Configuration reload now treats stored arrays as complete values instead of restoring deleted entries from fallback arrays, while retaining missing nested defaults and canonical configuration identity.

Verification: 10 real PostgreSQL cases passed (unowned data preservation, clears, concurrent/ABA conflict, current account/API authority, validation, HTTPS downgrade guard, rollback, activation failure/retry and history retention). Shared policy/schedule, HTTP transport, existing site/Security transport, configuration reload and announcement expiry/preview tests passed. Shared/client/server type checks, lint, Vite and bundle budgets passed. The General interface, legacy writer integration, wider reader/layout review and deployment remain pending. The live container stays on verified Security release `ad7c9186`, migration 20. The isolated `tsepistle-general-test` PostgreSQL container on port 55438 is available for continuing this work.
